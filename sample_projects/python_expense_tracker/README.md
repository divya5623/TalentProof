# Expense Tracker (demo project for Talent Proof)

Simple Python CLI expense tracker used in the Talent Proof hackathon demo.

## Features
- Add expenses with category and amount
- List expenses
- Reject invalid amounts
- Prevent duplicate transactions (same day + category + amount)

## Run tests
```bash
python -m unittest discover -s tests -v
```
